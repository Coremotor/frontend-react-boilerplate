export type TThemeState = {
  theme: string
  activeTab: string
  showNavMenu: boolean
}

export enum EnumTabs {
  main = 'main',
  theme = 'theme',
  language = 'language',
  pageThree = 'pageThree',
}
